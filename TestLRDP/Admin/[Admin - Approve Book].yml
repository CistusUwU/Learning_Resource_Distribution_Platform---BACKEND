info:
  name: "[Admin - Approve Book]"
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{BaseURL}}/api/admin/books/:bookId/approve"
  params:
    - name: bookId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
