info:
  name: "[Auth - Login Staff]"
  type: http
  seq: 1

http:
  method: POST
  url: "{{BaseURL}}/api/auth/login"
  body:
    type: json
    data: '{ "identifier": "GV001", "password": "password123", "role": "STAFF" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
