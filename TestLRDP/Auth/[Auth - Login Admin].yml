info:
  name: "[Auth - Login Admin]"
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseURL}}/api/auth/login"
  body:
    type: json
    data: '{ "identifier": "ADMIN001", "password": "password123", "role": "ADMIN" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
