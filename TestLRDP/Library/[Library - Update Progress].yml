info:
  name: "[Library - Update Progress]"
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/library/18/progress"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
